VITE_GOOGLE_MAPS_API_KEY=
VITE_DROPBOX_APP_KEY=
VITE_SENTRY_DSN=

# Optional CI override (commit SHA). Vercel defaults to VERCEL_GIT_COMMIT_SHA.
# Local builds generate a local-only ID when neither variable is supplied.
VITE_APP_RELEASE=
